export interface IAuthor{
    authorId: number,
    authorName: string,
    authorCode: string,
    releaseDate: string,
    description: string,
    // price:number,
    starRating: number,
    imageUrl: string
}
